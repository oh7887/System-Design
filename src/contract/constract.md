

```
关于合约的编译结果(也即"合约编译的artifacts"): 

ABI (Application Binary Interface): 这是一个 JSON 数组，描述了合约的方法、参数和其他数据结构。ABI 使得开发者可以创建和解析与合约交互的数据。

Bytecode: 这是合约的机器码，它会被部署到 Ethereum 区块链上。它是一个长字符串，通常表示为十六进制。

Contract Name: 合约的名称。

SourceMap: 用于调试的源映射。


```